#!/usr/bin/env python
# -*- coding: utf-8 -*-

def code(p):

    codigo = "INDIF"

    if p == "PP": codigo = "PP"
    if p == "PP-PAR": codigo = "PP"
    if p == "UPN-PP": codigo = "PP"
    if p == "PP-FORO": codigo = "PP"

    if p == "PODEMOS-AHAL DUGU": codigo = "PODEMOS"
    if p == "PODEMOS": codigo = "PODEMOS"
    if p == u"PODEMOS-COMPROMÍS": codigo = "PODEMOS"
    if p == "PODEMOS-COMPROMIS": codigo = "PODEMOS"
    if p == u"EN COMÚ": codigo = "PODEMOS"
    if p == u"PODEMOS-En Marea-ANOVA-EU": codigo = "PODEMOS"
    if p == u"PODEMOS-Ahora Alto Aragón en Común": codigo = "PODEMOS"
    if p == u"PODEMOS - COMPROMIS": codigo = "PODEMOS"
    if p == u"PODEMOS-EN MAREA-ANOVA-EU": codigo = "PODEMOS"

    if p == u"ERC-CATSÍ": codigo = "ERC"
    if p == "ERC-CATSI": codigo = "ERC"

    if p == "DL": codigo = "DL"

    if p == u"NÓS": codigo = "BNG"

    if p == u"GBAI": codigo = "GBAI"

    if p == "unio.cat": codigo = "Unio"
    if p == "Unio.Cat": codigo = "Unio"

    if p == "PSE-EE (PSOE)": codigo = "PSOE"
    if p == "PSE-EE": codigo = "PSOE"
    if p == "PSOE": codigo = "PSOE"
    if p == "PSC-PSOE": codigo = "PSOE"
    if p == "PSC": codigo = "PSOE"
    if p == "PSdeG-PSOE": codigo = "PSOE"
    if p == "PSOE-NCa": codigo = "PSOE"

    if p == "CCa-PNC": codigo = "CCa"

    if p == "C's": codigo = "C's"
    if p == u"C´s": codigo = "C's"

    if p == "IU-UPeC": codigo = "UP"
    if p == "UP-UPeC": codigo = "UP"
    if p == "EUPV-UPeC": codigo = "UP"
    if p == "IULV-CA,UPeC": codigo = "UP"
    if p == "UPB: IU-UPeC": codigo = "UP"
    if p == "IULV-CA, UPeC": codigo = "UP"
    if p == "UP: IULV-CA, UPeC": codigo = "UP"
    if p == "IU-CHA-UPeC": codigo = "UP"
    if p == "UPeC-IU": codigo = "UP"
    if p == "IULV-CA, UPeC": codigo = "UP"
    if p == "IU-B-UPeC": codigo = "UP"
    if p == "IU-UPeC-IAS": codigo = "UP"
    if p == "IUC-UPeC": codigo = "UP"
    if p == "UPeC-IU-IZCA": codigo = "UP"
    if p == "IU-UpeC": codigo = "UP"
    if p == "UNIDAD POPULAR": codigo = "UP"

    if p == "EAJ-PNV": codigo = "PNV"

    if p == "PACMA": codigo = "PACMA"

    if p == "UPYD": codigo = "UPYD"

    if p == "RECORTES CERO-GRUPO VERDE": codigo = "Recortes Cero"

    if p == "EB - AZ": codigo = "EB"
    if p == "EB-AZ": codigo = "EB"
    if p == "EB": codigo = "EB"

    if p == "EH Bildu": codigo = "Bildu"

    if p == "Vox": codigo = "VOX"
    if p == "VOX": codigo = "VOX"

    if p == "X LA IZQUIERDA-LOS VERDES": codigo = "Partido X"
    if p == "X ESQ": codigo = "Partido X"
    if p == "EZKERRA": codigo = "Partido X"

    if p == "ARAPV": codigo = "ARAPV"
    if p == "ARA, PV": codigo = "ARAPV"

    if p == "PCPE": codigo = "PC"
    if p == "PCPC": codigo = "PC"
    if p == "PCOE": codigo = "PC"

    if p == "OE": codigo = "OE"
    if p == "Ln": codigo = "Ln"
    if p == "PLD": codigo = "PLD"
    if p == "CENTRO MODERADO": codigo = "CM"
    if p == "FDEE": codigo = "FDEE"
    if p == "P-LIB": codigo = "P-LIB"
    if p == "SOMVAL": codigo = "SOMVAL"
    if p == "FE de las JONS": codigo= "FE"
    if p == "PUM+J": codigo= "PUM+J"
    if p == "DN": codigo= "DN"
    if p == "EU-eX": codigo ="EU-eX"
    if p == u"MÉS": codigo ="MES"
    if p == "PFyV": codigo = "PFyV"
    if p == "SAIn": codigo = "SAIn"
    if p == "IFem": codigo = "IFem"
    if p == "EL PI": codigo = "EL PI"
    if p == "SAIn": codigo = "SAIn"
    if p == "CCD": codigo = "CCD"
    if p == "PT": codigo = "PT"
    if p == "Independientes-FIA": codigo = "FIA"
    if p == "CILUS": codigo = "CILUS"
    if p == "AJU": codigo = "AJU"
    if p == "CRA": codigo = "CRA"
    if p == "PREPAL": codigo = "PREPAL"
    if p == "PH": codigo = "PH"
    if p == u"mlgXSÍ": codigo = "MALAGAPORSI"
    if p == "SOLUCIONA": codigo = "SOLUCIONA"
    if p == "+MAS+": codigo = "+MAS+"
    if p == "CANARIAS DECIDE": codigo = "Canarias Decide"
    if p == "EN POSITIU": codigo = "EN POSITIU"
    if p == "AVANT": codigo = "AVANT"
    if p == "JS,PC": codigo = "JS,PC"

    return codigo
